# Portfolio Vue (generated)

Run locally:

1. Install dependencies:
   npm install

2. Start dev server:
   npm run dev

This project uses:
- Vue 3
- Vite (vite.config.mjs - ESM)
- Tailwind CSS

Place your photo at src/assets/placeholder-photo.png (or update About.vue to a different path).
